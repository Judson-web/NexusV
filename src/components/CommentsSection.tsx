import React from "react";

export default function CommentsSection() {
  return (
    <div>
      <p className="text-sm text-gray-300">Comments are not persisted in this demo. Plug a backend (Firebase, Supabase) for full functionality.</p>
    </div>
  );
}